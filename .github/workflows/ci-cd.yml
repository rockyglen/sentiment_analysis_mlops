name: CI/CD Pipeline for Sentiment API

on:
  push:
    branches:
      - main

jobs:
  build_and_push_image:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Log in to Docker Hub
        uses: docker/login-action@v2
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Build and push Docker image
        uses: docker/build-push-action@v4
        with:
          context: .
          push: true
          tags: ${{ secrets.DOCKERHUB_USERNAME }}/sentiment-api:latest
          # Optional: Add a tag with the commit SHA for versioning
          # tags: ${{ secrets.DOCKERHUB_USERNAME }}/sentiment-api:latest, ${{ secrets.DOCKERHUB_USERNAME }}/sentiment-api:${{ github.sha }}